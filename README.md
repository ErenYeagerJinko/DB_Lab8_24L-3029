Install necessary dependencies by the following commands:
cd backend
npm init -y
npm install express cors mssql/msnodesqlv8
npm install tedious
npm install

To run:
cd backend
node server.js
(Open new terminal)
cd frontend
npm start
